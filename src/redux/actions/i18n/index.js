/**
 * Main export for i18n actions that will be used across the application.
 *
 * eg -
 *   import { i18nAction, i18nActionType } from './i18n';
 *
 * Note - Recommended to use de-structured actions from redux action's root exports 'src/redux/actions'
 *
 * eg -
 *   import { i18nAction, i18nActionType } from './redux/actions';
 */

import i18nAction from './action';
import i18nActionType from './type';

export { i18nAction, i18nActionType };
