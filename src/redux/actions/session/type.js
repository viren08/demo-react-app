/**
 * The file contents for the session redux action types that will be used by session redux actions,
 * reducer and sagas.
 *
 * All action type related to session actions must pe placed here only.
 */

export default {
  CURRENT_THEME: 'CURRENT_THEME'
};
