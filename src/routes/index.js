/**
 * Main export for route configuration that will be used by app component for rendering routes.
 *
 * eg -
 * import routes from './routes';
 */

import appRoutes from './app';

const routes = [...appRoutes];

export default routes;
